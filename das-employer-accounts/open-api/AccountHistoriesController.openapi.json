{
  "openapi": "3.1.0",
  "info": {
    "title": "Employer Accounts API",
    "version": "1.0.0"
  },
  "paths": {
    "/api/accounthistories": {
      "get": {
        "operationId": "GetAccountHistoryByRef",
        "summary": "Get account history by PAYE reference",
        "parameters": [
          {
            "name": "payeRef",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string"
            },
            "description": "The PAYE reference used to fetch account history."
          }
        ],
        "responses": {
          "200": {
            "description": "Account history data successfully retrieved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AccountHistory"
                }
              }
            }
          },
          "404": {
            "description": "Account history not found."
          }
        },
        "security": [
          {
            "ApiRoles": [
              "ReadAllEmployerAccountBalances"
            ]
          }
        ]
      }
    }
  },
  "components": {
    "schemas": {
      "AccountHistory": {
        "type": "object",
        "properties": {
          "AccountId": {
            "type": "string",
            "description": "The ID of the account."
          },
          "AddedDate": {
            "type": "string",
            "format": "date-time",
            "description": "The date the account was added."
          },
          "RemovedDate": {
            "type": "string",
            "format": "date-time",
            "description": "The date the account was removed."
          }
        },
        "required": [
          "AccountId",
          "AddedDate"
        ]
      }
    },
    "securitySchemes": {
      "ApiRoles": {
        "type": "apiKey",
        "name": "Authorization",
        "in": "header"
      }
    }
  }
}
