{
  "openapi": "3.1.0",
  "info": {
    "title": "Employer Accounts API",
    "version": "1.0.0"
  },
  "paths": {
    "/api/accounthistories": {
      "get": {
        "operationId": "GetAccountHistoryByRef",
        "summary": "Retrieve account history by PAYE reference",
        "parameters": [
          {
            "name": "payeRef",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string"
            },
            "description": "The PAYE reference to query account history for."
          }
        ],
        "responses": {
          "200": {
            "description": "Successful response with account history details.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AccountHistory"
                }
              }
            }
          },
          "404": {
            "description": "No account history found for the given PAYE reference."
          }
        },
        "security": [
          {
            "ApiRoles_ReadAllEmployerAccountBalances": []
          }
        ]
      }
    }
  },
  "components": {
    "schemas": {
      "AccountHistory": {
        "type": "object",
        "properties": {
          "AccountId": {
            "type": "integer",
            "description": "The unique identifier for the account."
          },
          "AddedDate": {
            "type": "string",
            "format": "date-time",
            "description": "The date the PAYE reference was added."
          },
          "RemovedDate": {
            "type": "string",
            "format": "date-time",
            "description": "The date the PAYE reference was removed."
          }
        }
      }
    },
    "securitySchemes": {
      "ApiRoles_ReadAllEmployerAccountBalances": {
        "type": "apiKey",
        "name": "Authorization",
        "in": "header"
      }
    }
  }
}
