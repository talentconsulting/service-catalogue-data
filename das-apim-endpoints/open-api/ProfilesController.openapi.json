{
  "openapi": "3.1.0",
  "info": {
    "title": "Admin AAN API",
    "version": "1.0.0"
  },
  "paths": {
    "/Profiles/{userType}": {
      "get": {
        "summary": "Get profiles by user type",
        "operationId": "GetProfiles",
        "parameters": [
          {
            "name": "userType",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            },
            "description": "Type of user for which profiles are fetched."
          }
        ],
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetProfilesResponse"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "GetProfilesResponse": {
        "type": "object",
        "properties": {
          "Profiles": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Profile"
            },
            "description": "List of profiles."
          }
        },
        "required": [
          "Profiles"
        ]
      },
      "Profile": {
        "type": "object",
        "properties": {
          "Id": {
            "type": "integer",
            "format": "int64",
            "description": "Unique identifier for the profile."
          },
          "Description": {
            "type": "string",
            "description": "Description of the profile."
          },
          "Category": {
            "type": "string",
            "description": "Category associated with the profile."
          },
          "Ordering": {
            "type": "integer",
            "description": "Ordering value for the profile."
          }
        },
        "required": [
          "Id",
          "Description",
          "Category",
          "Ordering"
        ]
      }
    }
  }
}
