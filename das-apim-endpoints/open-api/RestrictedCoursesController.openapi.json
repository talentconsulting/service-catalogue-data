{
  "openapi": "3.1.0",
  "info": {
    "title": "Restricted Courses API",
    "version": "1.0.0"
  },
  "paths": {
    "/restricted-courses": {
      "get": {
        "summary": "Get all restricted courses",
        "operationId": "GetRestrictedCourses",
        "parameters": [
          {
            "name": "restricted",
            "in": "query",
            "required": true,
            "schema": {
              "type": "boolean"
            },
            "description": "Filter courses based on their restriction status."
          }
        ],
        "responses": {
          "200": {
            "description": "A list of restricted courses.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetAllRestrictedCoursesResponse"
                }
              }
            }
          }
        }
      },
      "post": {
        "summary": "Add a restricted course",
        "operationId": "AddRestrictedCourse",
        "requestBody": {
          "description": "Details of the course to add.",
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddRestrictedCourseCommand"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Restricted course successfully added."
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "GetAllRestrictedCoursesResponse": {
        "type": "object",
        "properties": {
          "Courses": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RestrictedCourseModel"
            }
          }
        }
      },
      "AddRestrictedCourseCommand": {
        "type": "object",
        "properties": {
          "UserId": {
            "type": "string",
            "description": "The ID of the user adding the course."
          },
          "UserDisplayName": {
            "type": "string",
            "description": "The display name of the user adding the course."
          },
          "LarsCode": {
            "type": "string",
            "description": "The LARS code of the course to be restricted."
          }
        },
        "required": [
          "UserId",
          "UserDisplayName",
          "LarsCode"
        ]
      },
      "RestrictedCourseModel": {
        "type": "object",
        "properties": {
          "LarsCode": {
            "type": "string",
            "description": "The LARS code of the restricted course."
          },
          "Title": {
            "type": "string",
            "description": "The title of the restricted course."
          },
          "Level": {
            "type": "integer",
            "description": "The level of the restricted course."
          },
          "LearningType": {
            "type": "string",
            "description": "The type of learning for the restricted course."
          }
        },
        "required": [
          "LarsCode",
          "Title",
          "Level",
          "LearningType"
        ]
      }
    }
  }
}
