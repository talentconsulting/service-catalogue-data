# Copy this file to .env (gitignored) and set a real password to require
# HTTP Basic Auth credentials for every request. Leave AUTH_PASSWORD unset
# (or delete .env) to run the viewer without authentication, as before.
AUTH_USERNAME=admin
AUTH_PASSWORD=
