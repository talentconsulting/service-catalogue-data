services:
  catalogue-viewer:
    build: .
    ports:
      - "8080:8080"
    environment:
      CATALOGUE_DATA_DIR: /data
      AUTH_USERNAME: ${AUTH_USERNAME:-admin}
      AUTH_PASSWORD: ${AUTH_PASSWORD:-}
    volumes:
      - ..:/data:ro
    restart: unless-stopped
